{
    "name": "autosubtitlesapp",
    "version": "1.0.0",
    "description": "Auto subtitle generation, translation, and video rendering application",
    "scripts": {
        "start": "concurrently \"yarn server\" \"yarn client\"",
        "server": "cd server & npx nodemon index.js",
        "client": "cd client & yarn start",
        "install-all": "yarn install && cd client && yarn install && cd ../server && yarn install",
        "build": "cd client && yarn run build && cd .. && rimraf server/public && xcopy /E /I /Y client\\build server\\public"
    },
    "keywords": [
        "subtitles",
        "video",
        "ai",
        "translation"
    ],
    "author": "",
    "license": "MIT",
    "dependencies": {
        "concurrently": "^8.2.2",
        "dotenv": "^16.3.1"
    },
    "devDependencies": {
        "rimraf": "^5.0.5"
    }
}